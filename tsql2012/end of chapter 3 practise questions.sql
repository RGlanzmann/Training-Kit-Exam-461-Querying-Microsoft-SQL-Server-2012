from where group by having top select order by
from where group by having offset fetch select order by

skipping rows/paging
default deterministic query

what is needed for deterministic: include all rows that might have duplicate entries
in the order by list

with top: with ties and by using order by with all columns with duplicates
with offset-fetch the same